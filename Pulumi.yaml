name: verified-permissions-authorizer
description: A component deploying an AWS Verified Permissions policy and paired Lambda authorizer function
runtime: yaml
resources:
  # Create an AWS resource (S3 Bucket)
  my-bucket:
    type: aws:s3:BucketV2
outputs:
  # Export the name of the bucket
  bucketName: ${my-bucket.id}
config:
  pulumi:tags:
    value:
      pulumi:template: https://www.pulumi.com/ai/api/project/f2204e95-26c0-4f47-b139-76fd9028a85f.zip
